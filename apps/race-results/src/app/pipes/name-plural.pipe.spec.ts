import { NamePluralPipe } from './name-plural.pipe';

describe('NamePluralPipe', () => {
  it('create an instance', () => {
    const pipe = new NamePluralPipe();
    expect(pipe).toBeTruthy();
  });
});
