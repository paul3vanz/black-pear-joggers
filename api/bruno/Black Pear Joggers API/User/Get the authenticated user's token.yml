info:
  name: Get the authenticated user's token
  type: http
  seq: 3
  tags:
    - User

http:
  method: GET
  url: "{{baseUrl}}/user/token"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/user/token"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
