info:
  name: Get an athlete's runBritain rankings history
  type: http
  seq: 1
  tags:
    - Rankings

http:
  method: GET
  url: "{{baseUrl}}/rankings/:athleteId"
  params:
    - name: athleteId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/rankings/:athleteId"
      method: GET
      params:
        - name: athleteId
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
