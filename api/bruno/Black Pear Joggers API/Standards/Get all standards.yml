info:
  name: Get all standards
  type: http
  seq: 1
  tags:
    - Standards

http:
  method: GET
  url: "{{baseUrl}}/standards"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/standards"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
