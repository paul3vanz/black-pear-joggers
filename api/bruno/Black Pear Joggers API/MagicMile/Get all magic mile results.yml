info:
  name: Get all magic mile results
  type: http
  seq: 1
  tags:
    - MagicMile

http:
  method: GET
  url: "{{baseUrl}}/magicmile"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/magicmile"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
