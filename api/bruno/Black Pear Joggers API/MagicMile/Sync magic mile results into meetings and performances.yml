info:
  name: Sync magic mile results into meetings and performances
  type: http
  seq: 4
  tags:
    - MagicMile

http:
  method: GET
  url: "{{baseUrl}}/magicmile/syncmagicmile"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/magicmile/syncmagicmile"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
