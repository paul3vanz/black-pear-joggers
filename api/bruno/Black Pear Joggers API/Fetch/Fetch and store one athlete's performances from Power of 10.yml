info:
  name: Fetch and store one athlete's performances from Power of 10
  type: http
  seq: 5
  tags:
    - Fetch

http:
  method: GET
  url: "{{baseUrl}}/fetch/performances/:athleteId"
  params:
    - name: athleteId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/fetch/performances/:athleteId"
      method: GET
      params:
        - name: athleteId
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
