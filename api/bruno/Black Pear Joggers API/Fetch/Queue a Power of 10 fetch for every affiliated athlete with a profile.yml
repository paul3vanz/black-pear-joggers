info:
  name: Queue a Power of 10 fetch for every affiliated athlete with a profile
  type: http
  seq: 1
  tags:
    - Fetch

http:
  method: GET
  url: "{{baseUrl}}/fetch/athletes"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/fetch/athletes"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""

docs: Queue the nightly read for every affiliated member with a profile.
