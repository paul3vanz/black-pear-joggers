info:
  name: Fetch membership payments and store any new or changed records
  type: http
  seq: 3
  tags:
    - Fetch

http:
  method: GET
  url: "{{baseUrl}}/payments/fetch"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/payments/fetch"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
