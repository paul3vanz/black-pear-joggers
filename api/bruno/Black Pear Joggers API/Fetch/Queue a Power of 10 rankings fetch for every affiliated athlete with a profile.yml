info:
  name: Queue a Power of 10 rankings fetch for every affiliated athlete with a profile
  type: http
  seq: 7
  tags:
    - Fetch

http:
  method: GET
  url: "{{baseUrl}}/fetch/rankings"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/fetch/rankings"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
