info:
  name: Recalculate the isPersonalBest flag for all performances
  type: http
  seq: 6
  tags:
    - Fetch

http:
  method: GET
  url: "{{baseUrl}}/fetch/updatepersonalbests"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/fetch/updatepersonalbests"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
