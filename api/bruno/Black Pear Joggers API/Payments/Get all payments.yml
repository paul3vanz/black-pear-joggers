info:
  name: Get all payments
  type: http
  seq: 1
  tags:
    - Payments

http:
  method: GET
  url: "{{baseUrl}}/payments"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/payments"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
