info:
  name: Create registrations for club members not already tracked as athletes
  type: http
  seq: 4
  tags:
    - Registrations

http:
  method: GET
  url: "{{baseUrl}}/registrations/createregistrationsfrommemberships"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/registrations/createregistrationsfrommemberships"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
