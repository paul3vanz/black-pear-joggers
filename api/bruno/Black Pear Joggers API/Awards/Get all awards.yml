info:
  name: Get all awards
  type: http
  seq: 1
  tags:
    - Awards

http:
  method: GET
  url: "{{baseUrl}}/awards"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/awards"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
