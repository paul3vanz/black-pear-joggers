import { FinishTimePipe } from './finish-time.pipe';

describe('FinishTimePipe', () => {
  it('create an instance', () => {
    const pipe = new FinishTimePipe();
    expect(pipe).toBeTruthy();
  });
});
